import {localNavigation as service} from '../../../import'

export const namespace = '@ursip-' + service.name

export const PRESELECT_OPTION = `${namespace}/PRESELECT_OPTION`

export const SELECT_OPTION = `${namespace}/SELECT_OPTION`
